import 'package:flutter/widgets.dart';
import 'package:flutter/material.dart';

const double defaultPadding = 10.0; // standard padding using default items

const double defaultIconSize = 18; // default icons size using default items

const BorderRadius defaultBorderRadius = BorderRadius.all(
  Radius.circular(10),
); // default border radius
